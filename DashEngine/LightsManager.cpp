#include "LightsManager.h"

namespace DashEngine {
    /*
    void LightManager::SetDirectionalLight(DirectionalLight directionalLight)
    {
        m_directionalLight = directionalLight;
    }
    void LightManager::AddPointLight(PointLight pointlight)
    {
        m_pointLights.push_back(pointlight);
    }
    void LightManager::RemovePointLight(PointLight pointlight) {
        auto it = std::remove(m_pointLights.begin(), m_pointLights.end(), pointlight);
        m_pointLights.erase(it, m_pointLights.end());
    }

    void LightManager::AddSpotLight(SpotLight spotLight)
    {
        m_spotLights.push_back(spotLight);
    }
    void LightManager::RemoveSpotLight(SpotLight spotLight) {
        auto it = std::remove(m_spotLights.begin(), m_spotLights.end(), spotLight);
        m_spotLights.erase(it, m_spotLights.end());
    }
    */
}

